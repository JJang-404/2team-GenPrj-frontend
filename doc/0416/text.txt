'구도 선택' 에서 'WireframeChoiceCard' 를보면 '최종 광고 문구' 가 두줄에 있다던지
'Main Preview' 와 글씨체 , 글씨 크기가 다른점이 있습니다.
우선 'WireframeChoiceCard' 의 위치는 어디인가요?
최종적으로 Type2 는 '가게 이름' 은 가운데에서 왼쪽으로 오게,
Type3 은 '가게이름' 이 가장 하단에 왼쪽으로 오게, '광고 문구' 는 중앙에 있지만 사진보다 위쪽에 나오게(가장 위쪽은 아님. 공백은 기존과 동일하게 . 180 도 뒤집은것 처럼. 그렇다고 글자가 뒤집힌건 아님)
Type 4 는  Type3 과 '광고 문구' 위치는 동일하되, '가게이름'은 하단에 크게 키워서 멋스럽게 하고 싶습니다.
우선 수정말고 우선 'WireframeChoiceCard' 의 위치는 어디인지 알려주세요.
@analysis.md

---

Type1 Type2 는 하단으로, Type 3, Type4 는 타입별 슬로건 위치가 상단으로 갔으면 좋겠습니다.
그럼 어떻게 수정해야하나요? md 파일로 작성해주세요.

@reposition_plan.md

---

'전체 저장' 을 하게 되면 배경 외에 흰색 부분도 저장이 됩니다. 실제로 배경만 저장되게끔 수정해야합니다.
어떻게 수정해야하는지 md 파일로 알려주세요. 최대한 주석으로 원상복구 가능할 수 있게 수정하고 싶습니다.
(다른 팀원이 진행)

---

git 진행사항 작성합니다.
git 에서 feature1_7_patch 와 merge 했고 문제없음을 확인하고
feature1_7 에 push 합니다.

그리고 작업폴더는 feature1_8 에서 작업하겠습니다.

1. 변경 사항 확인

```
git status
```

2. 모든 수정 파일 스테이징
```
git add .
```

3. 커밋 (설명과 함께 저장)
```
git commit -m "fix: 전체 저장 여백 제거 및 레이아웃 충돌 해결"
```

4. 원격 저장소에 업로드
```
git push origin feature/United1_7
```

---

2단계: 다음 브랜치(feature/United1_8)로 이동
새 브랜치가 이미 원격(깃허브 등)에 있다고 하셨으니, 'clone' 대신 'checkout' 또는 'switch'를 사용합니다.

1. 원격 업데이트 정보 가져오기

```bash
git fetch origin
```
feature/United1_8 브랜치로 전환

```bash
git checkout feature/United1_8
```
(만약 이 명령어가 안 된다면 git switch -c feature/United1_8 origin/feature/United1_8을 시도해 보세요.)

2. 라이브러리 업데이트 (새 브랜치에 추가된 패키지가 있을 수 있으니 한 번 더 해주면 안전합니다.)

```bash
cd react
npm install
```